import {users, frames, artwork} from '../../src/sources/api.js';

describe('users', () => {

  it('use ', (done) => {

    const state = Object.freeze({});
    reducer(state, {type: 'INVALID'});

    done();
  });
});
